import { useUserData } from "@/components/UserContext";
import React from "react";

const Profile = () => {
  
  return (
    <div>
        <p>Show Profile</p>
    </div>
  );
}

export default Profile;
